# CodeOrbit_RuleBasedChatbot
A simple rule-based chatbot built with Python using predefined keywords, conditional logic, and basic pattern matching to handle greetings, common questions, and unknown inputs with appropriate responses.
